# Implementation Tasks

## Tasks (in execution order)

- [ ] 1. Add `aria-label` to icon buttons in `BudgetCard.tsx` (4 buttons: 2× edit, 2× delete)
- [ ] 2. Add `aria-label` to icon buttons in `BudgetsTable.tsx` (2 buttons: edit, delete)
- [ ] 3. Add `aria-label` to icon buttons in `CalendarGrid.tsx` (2 buttons: prev month, next month)
- [ ] 4. Add `aria-label` to icon buttons in `EntityTable.tsx` (2 buttons: edit, delete)
- [ ] 5. Add `aria-label` to icon buttons in `GroupedEntityTable.tsx` (2 buttons: edit, delete)
- [ ] 6. Add `aria-label` to icon buttons in `ManageSubCategoriesDialog.tsx` (4 buttons: save, cancel, edit, delete)
- [ ] 7. Add `aria-label` to icon button in `CurrencyManagement.tsx` (1 button: remove currency)
- [ ] 8. Add `aria-label` to icon buttons in `ScheduledBackups.tsx` (2 buttons: verify permission, delete schedule)
- [ ] 9. Run format, lint, type-check, and build to verify
- [ ] 10. Commit changes
